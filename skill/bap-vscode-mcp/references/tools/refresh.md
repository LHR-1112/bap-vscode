# refresh

对比本地与云端，重新计算变更列表。

## 参数

无。

## 示例

```json
{ "name": "refresh", "arguments": {} }
```

## 返回

非 NORMAL 变更数（按分组）。AI 先据此确定本地与云端差异，再决定提交 / 更新。

## 注意

- `refresh` 是只读计算，不改动本地或云端文件；确认差异后再做后续动作。
